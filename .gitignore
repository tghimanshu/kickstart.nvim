tags
test.sh
.luarc.json

spell/
lazy-lock.json

# tmux plugins are managed by TPM at runtime — not tracked in dotfiles
tmux/.config/tmux/plugins/

# stray vault directory that ended up inside dotfiles
notes/personal/
